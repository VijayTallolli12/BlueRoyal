# Dedicated Database Migration Runner for CI/CD Deployment Jobs
FROM node:22-alpine

WORKDIR /app
ENV NODE_ENV=production

COPY package*.json ./
COPY database/package*.json ./database/

RUN npm ci

COPY database ./database

# Default action runs pending migrations
CMD ["npm", "run", "migrate", "--workspace=database"]
